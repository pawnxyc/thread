1.采用数组加链表或者红黑树的结构。链表查询效率为O(n),而红黑树为log(n).
初始容量为2<<3，负载因子0.75，每次扩容为之前的2倍，当容量大于64并且链表长度大于6，就转换为红黑树。
hashmap在并发时可能会出现数据丢失的问题，列入执行list.next=e1,list.next=e2,只会保留后者。
concuurenthashmap采用多个链表的或者多个红黑树的分段锁方式来保证读写效率。